// Metodos de busqueda y transformacion 

function recorrerProductos(){
    // for - for of - forEach ---> Estos 3 parametros recorren un array

    Array.forEach(element => {
        // basicamente lo que hace .forEach es recorrer un array de punta a punta
        // he ingresa esos elementos en otro array, en este caso en element.
    })
}

// .find() sirve para buscar un objeto o elemento dentro de un array.

// .filter() sirve para filtrar los elementos de un array, te devuelve
// todas las coincidencias posibles dentro de un array.

// .some() es igual que "find" recorre un array para saber si existe el 
// elemento o no pero nos devuelve un resultado true o false.

// .map() sirve para recorrer todo el array y extraer los elementos que
// necesites de ese array y crear otro nuevo.

// .reduce() recibe 2 parametros, recorre un array y te devuelve el total
// del elemento que selecciones, por ejemplo el precio total de varios
// productos de un array.

// .sort() recibe 2 parametros, compara los elementos dentro de un array.
